

$(function() {
	$(".active").removeClass("active");
	$(".ico3").parent().addClass("active");

	/*
	 * var validator = $("#userform").validate({ rules: { username: { required:
	 * true, minlength: 4 }, password: { required: true, minlength: 5 },
	 * password_confirm: { required: true, minlength: 5, equalTo: "#password" },
	 * email: { required: true, email: true } }, messages: { username: {
	 * required: "Enter a username", minlength: jQuery.format("Enter at least
	 * {0} characters") }, password: { required: "Provide a password",
	 * rangelength: jQuery.format("Enter at least {0} characters") },
	 * password_confirm: { required: "Repeat your password", minlength:
	 * jQuery.format("Enter at least {0} characters"), equalTo: "Enter the same
	 * password as above" }, email: { required: "Please enter a valid email
	 * address", minlength: "Please enter a valid email address" }, }, // the
	 * errorPlacement has to take the layout into account errorPlacement:
	 * function(error, element) {
	 * error.insertAfter(element.parent().find('label:first')); }, // specifying
	 * a submitHandler prevents the default submit, good for the demo
	 * submitHandler: function() { alert("Data submitted!"); }, // set new class
	 * to error-labels to indicate valid fields success: function(label) { //
	 * set &nbsp; as text for IE label.html("&nbsp;").addClass("ok"); } });
	 */

});


